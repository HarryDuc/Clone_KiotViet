import ClientLayout from "../layouts/ClientLayout";

const Clients = () => {
    return (
        <ClientLayout>
            <h1 className="text-3xl font-bold">👥 Khách Hàng</h1>
            <p className="mt-4 text-gray-700">Chúng tôi đã hợp tác với nhiều khách hàng lớn trong ngành.</p>
        </ClientLayout>
    );
};

export default Clients;
